# Keep GitHub's language bar focused on the app code (TypeScript).
# styles.css is a single hand-tuned design layer (theme tokens, keyframe
# animations, syntax-highlight token colors, prose styles for generated HTML)
# that utility classes can't express inline, so it's marked as vendored.
src/styles.css linguist-vendored

# Generated by the TanStack Router plugin - not hand-written source.
src/routeTree.gen.ts linguist-generated

# Normalize line endings (silences the CRLF warnings on commit).
* text=auto eol=lf
